<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= data.title %> - Dev com</title>
</head>
<body>
    <% if(data.img) { %>
        <img src="<%= data.img %>" alt="<%= data.title %>">
    <% } %>
    <h1>Tag <%= data.title %></h1>
    <h3>Desk: <%= data.description %></h3>
    <p>Followers: <%= data.followers %></p>
</body>
</html>